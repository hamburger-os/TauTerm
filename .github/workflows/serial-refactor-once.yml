name: Serial final fixes one-shot

on:
  push:
    branches:
      - agent/serial-session-design-cleanup
    paths:
      - .github/workflows/serial-refactor-once.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/serial-session-design-cleanup
          fetch-depth: 0
      - name: Apply final lifecycle fixes
        run: python3 scripts/_serial_final_fixes.py
      - name: Format Rust
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --all
      - name: Commit generated changes
        run: |
          if git diff --quiet src-tauri; then
            echo "No product changes generated"
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src-tauri
          git commit -m "fix(serial): make virtual bridge startup atomic"
          git push origin HEAD:agent/serial-session-design-cleanup
      - name: Confirm product commit
        run: git log -1 --oneline
