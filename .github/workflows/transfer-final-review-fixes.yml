name: Transfer final review fixes

on:
  push:
    branches:
      - fix/transfer-role-aware-protocol-config
    paths:
      - scripts/apply_transfer_review_fixes.py
      - .github/workflows/transfer-final-review-fixes.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply review fixes
        run: python scripts/apply_transfer_review_fixes.py
      - name: Format Rust
        run: cargo fmt --manifest-path src-tauri/Cargo.toml
      - name: Commit product changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src src-tauri/src docs/modules/TRANSFER.md scripts/test-file-transfer-lifecycle.mjs
          git diff --cached --quiet && exit 0
          git commit -m "fix(transfer): correct modem protocol edge cases"
          git push
